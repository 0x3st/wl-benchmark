#!/usr/bin/env python3
"""WL-Benchmark CLI shim — runnable from anywhere the repo lives.

Usage:  ./wlb <command>  (from repo root, or symlink into PATH)
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from wl_benchmark.cli import main  # noqa: E402

if __name__ == "__main__":
    main()
